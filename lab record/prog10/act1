Import pandas as pd
from sk learn. preprocessing import one hot Emoder, label & mode off = pd.data Frame ({'OS': ['windows;'mas','Linux', 'window', Linux]'Storage':['HOD', 'SSD', 'SSD', 'HDD', "NUME'] })
One=One Hot Emoder (Sparse=Flase)
OS-Emoded=One.Fit-transform (df[['OS']])
OS-df= pd-Dataframe (OS_emoded, columns=One.get_feature_nams-out (['OS']))
le=label Emoder()
df ['storage_Emoded']=le.fit_transform (de['sotrage'])
emoded_df=pd.concat([df.drop(columns=['os'], os_df], oxis=1)
print('emoded data', emoded-df]

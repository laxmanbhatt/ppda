import pandas as pd
import matplot lip.pylot as plt
import seaborn as sns
df=pd.read_csv ("laptop.csv")
print("numeric columns", df.selectd_dtypes(include='number').columns.to list (1)
numeric=df.select_dtypes(include='number')
sns-set(style="white grid")
plt.figure(figsize=(12,6))
for ij co.in emmerate (numeric_dt.columns),);
plt.subplots (y=df[col], colour='skyblue')
plt.title (f"box.plot of{col}")
plt.tight_layout()
plt.show()

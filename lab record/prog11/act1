import pandas as pd
from sklearn.preprocessing import normalizes (), min maxscales()
df+pd.read_csv ("salary"csv")
numeric_df=df.select_dtypes(include='mube']
minmax = minmax scales()
df-minmax =pd.dataframe (minmax'fits_transforms (numric_df),columns'-numeric_df.columns)
print(df_minmax.head())
Normalius=Normalius()
df_normalized=pd.dataframe (normalise.fit transform (numeric_df), column= numeric_df.columns)
print(df_normalized.head())
